{% extends 'base.html.twig' %}

{% block title %}Participation{% endblock %}

{% block content %}
    <br>
    <br><br>
    <br><br>
    <h1>Participation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participation.id }}</td>
            </tr>
            <tr>
                <th>IdInternaute</th>
                <td>{{ participation.idInternaute }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ participation.type }}</td>
            </tr>
            <tr>
                <th>id voyage</th>
                <td>{{ participation.voy }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('participation_index') }}">back to list</a>

    <a href="{{ path('participation_edit', {'id': participation.id}) }}">modifier</a>

    {{ include('participation/_delete_form.html.twig') }}
{% endblock %}
