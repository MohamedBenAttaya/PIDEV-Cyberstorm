<form method="post" action="{{ path('partage_experience_delete', {'id': partage_experience.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partage_experience.id) }}">
    <button class="btn btn-dark">Delete</button>
</form>
