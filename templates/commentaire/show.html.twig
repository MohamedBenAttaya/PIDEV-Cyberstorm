{% extends 'base.html.twig' %}

{% block title %}Votre Commentaire :{% endblock %}

{% block content %}
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <div class="container">
    <h1>Commentaire</h1>
        <br>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Id Partage Experience</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ commentaire.id }}</td>
                <td>{{ commentaire.description }}</td>
                <td>{{ commentaire.partageexperience }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('commentaire_index') }}">Revenir a la liste des commentaires</a>

    <a class="btn btn-dark" href="{{ path('commentaire_edit', {'id': commentaire.id}) }}">Modifier</a>
<br>
        <br>
    {{ include('commentaire/_delete_form.html.twig') }}
    </div>
{% endblock %}
